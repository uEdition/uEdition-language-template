# User-provided settings
code:
  type: str
  help: What is the language code?

label:
  type: str
  help: What is the name of the language?

# Copier settings
_exclude:
  - copier.yml
  - .git
  - README.md
  - LICENSE
